// Файл вспомогательные ф-ии
'use strict';
(function () {
  window.utils = {};
  window.utils.getRandomInt = function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
  };
})();
